define([
  // Application.
  "app"
],

// Map dependencies from above array.
function(app) {

  // Create a new module.
  var Views = app.module();



  // Return the module for AMD compliance.
  return Views;

});
